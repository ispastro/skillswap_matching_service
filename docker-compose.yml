version: '3.8'

services:
  app:
    build: .
    ports:
      - "8001:8001"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_UPSTASH_REST_URL=${REDIS_UPSTASH_REST_URL}
      - REDIS_UPSTASH_REST_TOKEN=${REDIS_UPSTASH_REST_TOKEN}
